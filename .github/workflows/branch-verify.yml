name: Validate branch
on:
  pull_request:
    types:
      - synchronize
      - opened

jobs:
  pr-verify-maven:
    uses: entur/abt-gha-public/.github/workflows/maven-open-source-verify.yml@main
    with:
      java-version: 17

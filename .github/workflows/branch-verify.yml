name: Validate branch
on:
  pull_request:
    types:
      - synchronize
      - opened

jobs:
  pr-verify-maven:
    uses: entur/abt-gha-public/.github/workflows/maven-open-source-verify.yml@vx.x.x
    with:
      java-version: 17
